import { HighlightFavorite } from './highlight-favorite.directive';

describe('HighlightFavorite', () => {
  it('should create an instance', () => {
    const directive = new HighlightFavorite();
    expect(directive).toBeTruthy();
  });
});
